---
apiVersion: v1
kind: Service
metadata:
  name: scaletest-service
  labels:
    app: scaletest-service
spec:
  selector:
    app: scaletest-service
  ports:
  - port: 8080
    targetPort: 8080
    name: metrics
  type: NodePort
...
